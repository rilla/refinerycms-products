<% if searching? %>
  <h2><%= t('results_for', :scope => 'shared.admin.search', :query => params[:search]) %></h2>
<% end %>
<% if @products.any? %>
  <%= render :partial => 'sortable_list' %>
<% else %>
  <p>
    <% unless searching? %>
      <strong><%=t('.no_products_yet')%></strong>
    <% else %>
      <%= t('no_results', :scope => 'shared.admin.search') %>
    <% end %>
  </p>
<% end %>
